{
    "extends": ["//"],
    "tasks": {
        "build": {
            "outputs": ["dist/**"],
            "env": [
                "USER_JWT_SECRET",
                "HTTP_PORT",
                "WS_PORT"
            ]
        }
    }
}